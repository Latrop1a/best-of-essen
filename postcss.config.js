const autoprefixer = require('autoprefixer');

module.exports = {
  plugins: [
    [
      'postcss-preset-env',
      {
        // Options
      },
    ],
  ],
};

module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
